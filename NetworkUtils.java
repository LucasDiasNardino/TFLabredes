import java.util.ArrayList;
import java.util.List;

public class NetworkUtils {
    public static List<String> calculateIPRange(String subnet) {
        // Implementar cálculo de IP range para a subnet especificada
        List<String> ips = new ArrayList<>();
        // Exemplo de preenchimento do range de IPs
        return ips;
    }

    public static byte[] getMacAddress(String ipAddress) {
        // Implementar resolução de MAC Address
        return new byte[]{};
    }
}
